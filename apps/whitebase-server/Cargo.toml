[package]
name = "whitebase-server"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
axum = "0.8"

tokio = {
    version = "1",
    features = [
        "macros",
        "net",
        "rt-multi-thread",
    ],
}

whitebase-http-api = {
    path = "../../crates/interface/whitebase-http-api"
}